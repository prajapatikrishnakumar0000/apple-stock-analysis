# Stock Price Analysis (AAPL)

## Project Requirements
- Python 3.10.x (required)
- Virtual Environment: venv

## Libraries Used
See `requirements.txt' 

## Setup Instructions 
```open new terminal -> select cmd 

firstly install python 3.10 version in your PC 

1. py -3.10 -m venv venv          
         or (if 3.10 is not present )
    python -m venv venv
2. venv\Scripts\activate      # Windows
3. pip install -r requirements.txt

## dataset fetched by using python and API 
 code for fetching : see " fetch_data.py"

 ## note: 
 after every new file creation :
 1. activate venv
 2. select kernel -> python enviroment -> *venv (3.10.x)
